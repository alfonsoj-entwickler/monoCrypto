<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="systemcryptography.monocrypto" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_mono"
        android:label="@string/app_name"
        android:logo="@drawable/icon_main"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".enter_Text_Activity"
            android:label="@string/title_activity_enter__text_" >
        </activity>
        <activity android:name=".AboutActivity" >
        </activity>
        <activity
            android:name=".InformationActivity"
            android:label="@string/mainMenuSecondary5" >
        </activity>
    </application>

</manifest>
